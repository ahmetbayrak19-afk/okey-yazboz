# Bu workflow devre dışı bırakıldı.
# Artık Capacitor kullanıyoruz, GitHub Pages'e gerek yok.

name: Disabled Pages Deploy

on:
  workflow_dispatch:  # Sadece manuel çalıştırılabilir (otomatik çalışmaz)

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "Pages workflow disabled - using Capacitor instead"